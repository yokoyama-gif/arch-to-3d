/**
 * キャンバス（作業台）の既定寸法。
 * A3横（420×297mm）を 1/100 縮尺で読み込むことを想定し、実寸換算で
 *  幅 42000mm × 高さ 29700mm のサイズを規定する。
 *
 * これにより、A3で出力された平面図PDF/画像をアップロードすると、
 * キャンバス全体にぴったり収まる構成になる。
 */
export const CANVAS_DEFAULTS = {
  /** キャンバス幅(mm) - A3横の長辺 × 100 */
  widthMm: 42000,
  /** キャンバス高さ(mm) - A3横の短辺 × 100 */
  heightMm: 29700,
  /** 起動時の表示倍率 - 42000mm × 0.04 = 1680px (一般的な画面に収まる) */
  defaultScale: 0.04,
} as const;
